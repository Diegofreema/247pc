import { StyleSheet, View, Text } from 'react-native';

type Props = {};

export const TopComponent = ({}: Props): JSX.Element => {
  return (
    <View>
      <Text> TopComponent</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
